# MuseTalk inference deps (from upstream requirements.txt; gradio dropped -- no UI in a module).
# NOTE: upstream targets torch 2.0.1, but this image runs torch 2.8 / cu128 (mandatory for RunPod's
# Blackwell cards -- see the Dockerfile). diffusers/transformers pins may need a bump on that newer
# torch; that, plus mmcv/mmpose (installed via openmim in the Dockerfile), is what Phase 0 burns down.
diffusers==0.30.2
accelerate==0.28.0
numpy==1.23.5
tensorflow==2.12.0
tensorboard==2.12.0
opencv-python==4.9.0.80
soundfile==0.12.1
transformers==4.39.2
huggingface_hub==0.30.2
librosa==0.11.0
einops==0.8.1
omegaconf
ffmpeg-python
moviepy
imageio[ffmpeg]
gdown

# --- module runtime (the RunPod / R2 contract) ---
runpod
boto3
requests
pyyaml
