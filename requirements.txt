# NOTE: the real install is STAGED in the Dockerfile -- order + flags (--no-deps, --no-build-isolation,
# MMCV_WITH_OPS, the version pins) matter, and a flat `pip install -r requirements.txt` will NOT
# reproduce a working environment. This file is a human-readable manifest of the proven pin set,
# validated end-to-end in Phase 0 on a live L40S (cu128/torch2.8/py3.12, 2026-06-20).
#
# build / py3.12 fixes:
#   setuptools==75.6.0  wheel  ninja  cython  numpy  pillow  scipy  matplotlib
#
# openmmlab (no prebuilt mmcv for cu128/torch2.8 -> mmcv built from source; chumpy OMITTED on purpose):
#   mmengine>=0.10   mmcv==2.1.0   mmpose==1.3.2   mmdet==3.2.0
#   hand-installed mm deps: json_tricks  munkres  pycocotools  terminaltables  shapely  xtcocotools
#
# MuseTalk inference (huggingface_hub PINNED <1.0 -- 1.x breaks transformers 4.39.2 + the CLI):
#   diffusers==0.30.2  accelerate==0.28.0  transformers==4.39.2  huggingface_hub==0.25.2
#   omegaconf  librosa  soundfile  opencv-python  einops  imageio[ffmpeg]  ffmpeg-python  moviepy  gdown
#
# module runtime (the RunPod / R2 contract):
#   runpod  boto3  pyyaml
#
# runtime env (set in the Dockerfile): TORCH_FORCE_NO_WEIGHTS_ONLY_LOAD=1 (torch 2.6+ load fix),
#   CUDA_HOME=/usr/local/cuda, HF_HUB_OFFLINE=1 / TRANSFORMERS_OFFLINE=1 (weights are baked).
